import {FC, memo} from 'react';
import AdminPanel from '../components/Admin/AdminPanel';

const Admin: FC = memo(() => {
  return (
    <div>
      <AdminPanel />
    </div>
  );
});

export default Admin;
